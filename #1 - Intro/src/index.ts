export {};

let name: string = "Vica";

const programmingLanguages: string[] = [];

programmingLanguages.push("PHP");

let result: string | number = "OK";

interface User {
    name: string;
    age: number
}

const user: User = {
    name: "Vica",
    age: 28,
};
